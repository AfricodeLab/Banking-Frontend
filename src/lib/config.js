/**
 * App-wide configuration. Change BASE_CURRENCY here to re-default the whole app.
 */
export const BASE_CURRENCY = 'GHS';

// Currencies offered in pickers (base currency first).
export const CURRENCIES = ['GHS', 'USD', 'EUR', 'GBP', 'NGN'];
